import React from 'react';
import GithubLab from '@components/githublab';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<GithubLab />);
